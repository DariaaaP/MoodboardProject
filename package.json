{
  "name": "moodboardproject",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "server": "json-server --watch db.json --port 3001",
    "watch": "parcel index.html --open --port 3000"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@parcel/transformer-sass": "^2.7.0",
    "parcel": "^2.7.0",
    "sass": "^1.55.0"
  },
  "dependencies": {
    "json-server": "^0.17.0"
  }
}
